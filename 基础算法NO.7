#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int isprime[n+5],i;
    for(i=1;i<=n;i++)
        isprime[i]=i;
    isprime[1]=0;
    isprime[2]=1;//为1时表示为素数
    for(int j=2;j<=n;j++)
    {
        if(isprime[j]%2)//为了减小循环次数
            isprime[j]=1;
        else isprime[j]=0;
        if(isprime[j])//筛法求素数
        {
            for(i=3;i*j<=n;i++)//i=3，可以改为i=2，但是上一步依据排除了2的倍数，为避免重复，所以改为i=3
                isprime[i*j]=0;
        }
    }
    for(i=1;i<=n;i++)
    {
        if(isprime[i])
            printf("%d\t",i);
    }
    printf("\n");
    return 0;
}
